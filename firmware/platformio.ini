; Optional: build/run locally with PlatformIO + the Wokwi extension.
; (The primary path is the Wokwi web playground; see README.md.)
[env:esp32dev]
platform      = espressif32
board         = esp32dev
framework     = arduino
monitor_speed = 115200
lib_deps      = beegee-tokyo/DHT sensor library for ESPx
